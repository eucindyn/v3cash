import styled from "styled-components";
import iconLogo from "../../assets/icon-logo.png";

const IconLogo = styled.img.attrs({ src: iconLogo, alt: "Icon Logo" })`
  width: 2.125rem;
  image-rendering: pixelated;
`;

export default IconLogo;
