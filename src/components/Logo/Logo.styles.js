import styled from "styled-components";

const Name = styled.figcaption``;

const Logo = styled.figure`
  display: flex;
  align-items: center;
  gap: 0.5rem;
  color: var(--color-txt);
`;

export default {
  Logo,
  Name
};
